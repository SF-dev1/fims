"use strict";

var Vendors = function () {

	return {
		init: function (type) {
			switch (type) {
				case "inventory":
					break;
			}
		}
	}
}();

function imgError($this) {
	$this.src = "https://via.placeholder.com/100x100";
}